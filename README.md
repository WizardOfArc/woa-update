# WOA Updater
This code handles updating the Wizard of Arc site. By rendering the countdown CSV into the time.json and rendering the tera templates with all the JSON and then updating all the involved repos.  I'm separating this out so that it can be called from the commandline on it's own instead of having to make a blog entry each time.
